defaults:
  - base_dqn

name: dqn
learning_rate: 0.001
gamma: 0.99
epsilon_start: 1.0
epsilon_end: 0.1
epsilon_decay: 0.995
batch_size: 32
replay_buffer_size: 5000
train_after: 100
train_every: 1
target_update_freq: 200
max_grad_norm: 10.0
normalize_obs: true
device: auto
